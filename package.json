{
  "name": "phonebook-fullstack",
  "version": "1.0.0",
  "description": "A fullstack application that manages the phone numbers of user",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "dev": "nodemon app.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf build && cd ../../task2/phone-book && npm run build --prod && cp -r build ../../task3/phonebook-fullstack",
    "deploy": "git push heroku master",
    "deploy:commit": "git add . && git commit -m server-deploy && git push && git push heroku master",
    "deploy:full": "npm run build:ui && git add . && git commit -m server-deploy-uibuild && git push && npm run deploy",
    "logs:prod": "heroku logs --tail"
  },
  "author": "Oluwatobi Oyejide",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.4"
  },
  "nodemonConfig": {
    "restartable": "rs",
    "ignore": [
      "node_modules/**/node_modules"
    ],
    "delay": 2500,
    "env": {
      "NODE_ENV": "development",
      "PORT": 3001
    }
  }
}
